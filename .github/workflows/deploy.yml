name: Deploy to Lightsail

on:
  push:
    branches:
      - producao  # Substitua pela branch que deseja monitorar

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Check out code
        uses: actions/checkout@v3

      - name: Set up SSH
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.SSH_KEY }}" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa  # Corrige as permissões da chave privada
          ssh-keyscan -H "${{ secrets.LIGHTSAIL_HOST }}" >> ~/.ssh/known_hosts

      - name: Deploy to Lightsail
        run: |
          ssh -o StrictHostKeyChecking=no ${{ secrets.USERNAME }}@${{ secrets.LIGHTSAIL_HOST }} << 'EOF'
            # Caminho para o seu projeto no servidor
            cd /home/ubuntu/lp-af-saude-unimed
            # Baixar a versão mais recente do código
            git pull origin producao
            # Ativar ambiente virtual (se necessário)
            source linux-venv/bin/activate
            # Instalar dependências
            pip install -r requirements.txt
            # Executar migrações (se for Django)
            python manage.py migrate
            # Coletar arquivos estáticos (se for Django)
            python manage.py collectstatic --noinput
            # Reiniciar Gunicorn
            sudo systemctl restart gunicorn
            # Sair do diretório do projeto
            cd ..
            # Reiniciar Nginx
            sudo systemctl restart nginx
          EOF